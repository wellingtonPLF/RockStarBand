export declare const JwtType: {
    NOT_AUTHORIZED: string;
    INVALID_USER: string;
    INVALID_AT: string;
    INVALID_RT: string;
    EXPIRED_AT: string;
    EXPIRED_RT: string;
};
export declare enum TokenType {
    AT_NAME = "access_token",
    RT_NAME = "refresh_token",
    ACCESS_TOKEN = 1,
    REFRESH_TOKEN = 720
}
